apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: {{ .Release.Name }}-logs-pvc

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: {{ .Values.persistence.logs.size }}